# 倍朗达官网多语言 SEO/GEO 实施计划

> **给代理执行者：** 推荐在本会话内按勾选逐步执行，并在每个阶段运行验证命令。任务使用 `- [ ]` 勾选跟踪。

**目标：** 交付一套可部署到 IIS 的中文、英文、俄语工业自动化官网静态站点。

**架构要点：** 内容与页面模板分离，通过 Node 构建脚本生成三种语言的静态 HTML；语言路径独立，页面源码直接包含主要内容；前端 JavaScript 只负责交互增强，不承载 SEO/GEO 核心内容。

**技术栈：** HTML5、CSS、原生 JavaScript、Node.js 内置 `node:test`；验证命令为 `node --test tests/*.test.mjs` 和 `node tools/build-site.mjs`。

**关联设计文档：** `docs/specs/2026-09-03-官网多语言SEO-GEO设计.md`

---

### 任务 1：建立多语言公共接口与失败测试

**涉及文件：**
- 新建：`site/src/i18n.mjs`
- 新建：`tests/i18n.test.mjs`

- [ ] **步骤 1：编写失败测试**

测试 `supportedLocales`、浏览器语言解析、缺失语言回退和路径生成行为。

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { supportedLocales, resolveLocale, localePath } from '../site/src/i18n.mjs';

test('支持中文、英文和俄语', () => {
  assert.deepEqual(supportedLocales, ['zh-cn', 'en', 'ru']);
});

test('浏览器语言匹配到俄语', () => {
  assert.equal(resolveLocale({ browserLanguages: ['ru-RU'], storedLocale: null }), 'ru');
});

test('未知语言回退中文', () => {
  assert.equal(resolveLocale({ browserLanguages: ['de-DE'], storedLocale: null }), 'zh-cn');
});

test('语言路径保持当前页面', () => {
  assert.equal(localePath('en', '/products/automatic-batching/'), '/en/products/automatic-batching/');
});
```

- [ ] **步骤 2：运行测试确认失败**

运行：`node --test tests/i18n.test.mjs`

预期：因 `site/src/i18n.mjs` 尚不存在而失败，失败原因必须是模块未找到，而不是测试语法错误。

- [ ] **步骤 3：实现最小接口**

实现 `supportedLocales`、`resolveLocale({ browserLanguages, storedLocale })`、`localePath(locale, pathname)`。存储语言优先于浏览器语言，未知语言回退 `zh-cn`，路径去重语言前缀后再添加目标前缀。

- [ ] **步骤 4：运行测试确认通过**

运行：`node --test tests/i18n.test.mjs`

预期：4 个测试全部通过。

### 任务 2：建立内容模型和三语言页面生成器

**涉及文件：**
- 新建：`site/src/content.mjs`
- 新建：`site/src/template.mjs`
- 新建：`tools/build-site.mjs`
- 新建：`tests/build-site.test.mjs`

- [ ] **步骤 1：编写失败测试**

测试构建结果必须含三种语言目录、唯一页面标题、canonical、hreflang、Organization、Product 和 BreadcrumbList。

- [ ] **步骤 2：运行测试确认失败**

运行：`node --test tests/build-site.test.mjs`

预期：因构建器和生成目录不存在而失败。

- [ ] **步骤 3：实现最小构建器**

在 `content.mjs` 中定义中文、英文、俄语的导航、首屏、解决方案、产品卡片、案例、FAQ、联系信息和 SEO 元数据；`template.mjs` 负责输出语义化 HTML 和 JSON-LD；`build-site.mjs` 生成 `site/zh-cn/index.html`、`site/en/index.html`、`site/ru/index.html` 及对应静态资源。

- [ ] **步骤 4：运行测试确认通过**

运行：`node tools/build-site.mjs; node --test tests/*.test.mjs`

预期：构建成功，所有测试通过，三种语言页面均生成。

### 任务 3：实现现代化响应式视觉层和交互

**涉及文件：**
- 新建：`site/src/styles.css`
- 新建：`site/src/app.mjs`
- 修改：`site/src/template.mjs`
- 测试：`tests/build-site.test.mjs`

- [ ] **步骤 1：补充失败测试**

验证生成页面包含 viewport、移动端导航按钮、语言选择器、主 CTA、跳过导航链接和图片尺寸属性。

- [ ] **步骤 2：运行测试确认失败**

运行：`node --test tests/build-site.test.mjs`

预期：旧模板缺少相应标记而失败。

- [ ] **步骤 3：实现响应式页面**

使用深蓝、石墨灰、工业橙的视觉系统；首页包含首屏价值主张、行业解决方案、核心产品、项目数据、案例、知识内容和咨询 CTA；CSS 使用流式容器、Grid/Flex 和移动端断点，不使用固定 1002px 页面宽度；JavaScript 实现移动菜单、语言切换和 FAQ 展开。

- [ ] **步骤 4：运行测试确认通过**

运行：`node tools/build-site.mjs; node --test tests/*.test.mjs`

预期：所有测试通过。

### 任务 4：补齐 SEO/GEO 基础文件和内容结构

**涉及文件：**
- 新建：`site/robots.txt`
- 新建：`site/sitemap.xml`
- 修改：`tools/build-site.mjs`
- 修改：`site/src/content.mjs`
- 测试：`tests/build-site.test.mjs`

- [ ] **步骤 1：编写失败测试**

验证 sitemap 覆盖三种语言首页，robots.txt 指向 sitemap，页面包含可见 FAQ、更新时间、实体信息、产品参数和资料入口。

- [ ] **步骤 2：运行测试确认失败**

运行：`node --test tests/*.test.mjs`

预期：SEO/GEO 文件或字段缺失导致断言失败。

- [ ] **步骤 3：实现 SEO/GEO 内容层**

生成三语言 sitemap 和 robots.txt；在页面中提供公司实体、行业术语中英俄映射、产品参数、FAQ、案例事实、技术资料占位入口和更新时间；所有结构化数据必须与页面可见内容一致。

- [ ] **步骤 4：运行测试确认通过**

运行：`node tools/build-site.mjs; node --test tests/*.test.mjs`

预期：构建成功，SEO/GEO 断言全部通过。

### 任务 5：交付前检查与部署说明

**涉及文件：**
- 新建：`README.md`
- 新建：`site/web.config`
- 新建：`tests/security-content.test.mjs`

- [ ] **步骤 1：编写失败测试**

验证生成站点不含 `tokenpocket`、未知外部脚本、旧站 404 链接，且 `web.config` 支持默认文档、压缩、缓存和 HTTPS 跳转配置说明。

- [ ] **步骤 2：运行测试确认失败**

运行：`node --test tests/security-content.test.mjs`

预期：在站点资源完成前因目录或文件不存在而失败。

- [ ] **步骤 3：实现部署配置和检查脚本**

写入 IIS 静态站点部署说明、MIME 类型、HTTPS 迁移注意事项和上线检查清单；测试只检查本地生成内容，不执行线上写入。

- [ ] **步骤 4：运行全量验证**

运行：`node tools/build-site.mjs; node --test tests/*.test.mjs`

预期：全部测试通过；生成目录中三种语言页面、robots.txt、sitemap.xml 和 web.config 齐全。
